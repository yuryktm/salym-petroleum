import { MaskEditFieldPipe } from './mask-edit-field.pipe';

describe('MaskEditFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new MaskEditFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
